# Laisky Blog OpenAPI

The machine-readable OpenAPI document is available at `/openapi.json`.

It describes public read-only resources for agents, including `/api`, `/v1/resources`, `/v1/batch/resources`, `/v1/jobs/{job_id}`, `/ask`, `/llms.txt`, `/index.md`, and MCP discovery metadata.
